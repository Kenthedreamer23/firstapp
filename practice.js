// Adding two numbers

var num1, num2

function addTwoNumbers(num1, num2) {
    return(num1 + num2)
}

console.log(addTwoNumbers(10,10));

// Convert minutes in seconds

var x;

function convertMin(x) {
    return(x*60)
}

console.log(convertMin(3));
